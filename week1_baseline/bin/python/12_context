#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/../../python/12_context"

# Prefer the lesson-local virtualenv if it exists, else fall back to python3.
if [ -x ".venv/bin/python" ]; then
  PY=".venv/bin/python"
else
  PY="python3"
fi

exec "$PY" examples/repl.py "$@"
