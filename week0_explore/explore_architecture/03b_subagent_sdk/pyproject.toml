[project]
name = "play-mud-agent-sdk"
version = "0.1.0"
description = "Architecture 3B: MUD-playing sub-agent driven by the Claude Agent SDK"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.0",
]
